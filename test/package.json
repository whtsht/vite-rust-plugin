{
  "name": "vue-vite-template",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "clean": "rimraf dist && rimraf pkg",
    "reset": "npm run clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "vite": "^2.8.4",
    "vite-rust-plugin": "file:.."
  }
}
